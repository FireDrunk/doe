---
dependency:
  name: galaxy

driver:
  name: podman

platforms:
  - name: instance
    image: ubi8/ubi-init
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
    command: "/usr/sbin/init"

provisioner:
  name: ansible

verifier:
  name: ansible

ansible:
  sudo: False